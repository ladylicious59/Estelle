{
  "name": "persona-ai",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web & npm run dev -w apps/server",
    "build": "npm run build -w apps/web & npm run build -w apps/server"
  }
}
